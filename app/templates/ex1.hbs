{{page-title "Ex1"}}
<div class="card">
  <div class="card-header bg-primary text-light">
    Ex 1 : Note application
  </div>
  <div class="card-body">
    <Textarea class="form-control mb-3" rows="4" maxlength="100" @value={{this.content}} @input={{this.update}}/>

    <button class="btn btn-primary" type="button" {{on "click" this.routsave}}>Sauvegarder</button>
    <button class="btn btn-primary" type="button" {{on "click" this.clear}}>Effacer</button>

    <div class="alert alert-info">Il vous rest {{this.size}} caractères à saisir</div>
    <div class={{this.style}}> {{this.info}}</div>
  </div>
</div>
{{outlet}}